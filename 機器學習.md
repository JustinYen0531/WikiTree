# 🌳 WikiTree 知識節點：機器學習（Machine Learning, ML）

```text
[根 Root] 機器學習本質：從「寫死規則」到「數據中歸納權重」
  │
  ├── [主幹 Trunk 01] 傳統開發 vs 機器學習（遊戲視角）
  │
  ├── [主幹 Trunk 02] 三大派系分支
  │     ├─ 監督式學習 (Supervised)
  │     ├─ 非監督式學習 (Unsupervised)
  │     └─ 強化學習 (Reinforcement)
  │
  ├── [主幹 Trunk 03] 底層閉環迴圈（The Core Loop）
  │
  └── [邊界 Bounds] 常見掉坑與 Bug（Overfitting / GIGO）
```

---

### 🌱 根節點：什麼是機器學習？
- **一句話定義**：不靠工程師手寫死規則（Hard-coded rules），而是給予大量數據與目標，讓演算法**「自己找出輸入與輸出之間的對應公式（權重）」**。
- **傳統程式設計**：`輸入資料` + `程式邏輯 (If-Else / 行為樹)` $\longrightarrow$ **產生結果**
- **機器學習**：`輸入資料` + `期望結果` $\longrightarrow$ **自主訓練出「邏輯模型」**

---

### 🌿 主幹 01：遊戲開發者的直覺對照

| 比較維度 | 傳統遊戲腳本（Scripted AI） | 機器學習（ML-based AI） |
| :--- | :--- | :--- |
| **實現方式** | 策劃手寫 FSM（有限狀態機）或 Behavior Tree | 餵給模型十萬場對戰錄影或讓它自弈 |
| **邏輯特點** | `if (HP < 20%) drink_potion();` 清楚直觀但呆板 | 輸出各動作的「機率分佈」，會走出沒寫過的風騷走位 |
| **調整維護** | 策劃逐條改數值、補分支條件 | 調整超參數（Hyperparameters）、清洗補充數據重訓 |

---

### 🌿 主幹 02：三大核心修練分支

#### 1. 監督式學習（Supervised Learning）——「開卷考 / 附解答的題庫」
- **概念**：每筆資料都有標籤（Label / Ground Truth）。
- **遊戲比喻**：餵給 AI 10 萬張裝備截圖，每張都標註「傳奇」或「垃圾」，訓練它學會一眼鑑定神裝。
- **主要任務**：
- **分類（Classification）**：二分法或多元判斷（如：外掛偵測、玩家流失預測）。
- **回歸（Regression）**：預測連續數值（如：預測次日留存率、首週營收）。

#### 2. 非監督式學習（Unsupervised Learning）——「盲盒探索 / 摸索聚類」
- **概念**：資料沒有預先給標籤，由演算法自己找出資料群體的隱在規律或分群。
- **遊戲比喻**：把伺服器 50 萬名玩家的行為日誌丟進去，AI 自己分出「課金外觀黨」、「PVP狂人」、「純社交養老族」。
- **主要任務**：分群（Clustering）、降維特徵壓縮（Dimensionality Reduction）。

#### 3. 強化學習（Reinforcement Learning, RL）——「NPC 刷副本練功」
- **概念**：沒有固定答案，只有 **環境（Environment）**、**動作（Action）** 與 **獎懲分數（Reward）**。
- **遊戲比喻**：打王掉血扣分、成功招架加分、通關給大獎。讓 AI 死亡一千萬次後，自己練出無傷通關的極限操作（如 AlphaStar、OpenAI Five）。

---

### ⚙️ 主幹 03：核心運作閉環（Game Loop）

所有機器學習本質上都在跑這個迴圈：

$$\text{資料 (Data)} \xrightarrow{\text{前向預測}} \text{預測值} \xrightarrow{\text{損失函數 Loss}} \text{計算誤差} \xrightarrow{\text{梯度下降/優化器}} \text{微調權重 Weights}$$

1. **Model（模型架構）**：角色的空白屬性天賦樹。
2. **Loss Function（損失函數）**：衡量「猜得有多爛」的扣分機制（扣越少越準）。
3. **Optimizer（優化器）**：根據扣分方向，自動微調每個天賦節點數值（權重調整）。

---

### ⚠️ 邊界節點：開發避坑指南

1. **過擬合（Overfitting）**：
- *症狀*：NPC 把題庫死記硬背了，在測試關卡 100 分，上線遇到新玩家被虐成智障。
- *解法*：增加數據多樣性、正則化（Regularization）、Dropout。
2. **GIGO（Garbage In, Garbage Out）**：
- 數據品質決定模型上限；演算法只是逼近那個上限。數據髒，模型必爛。
3. **黑盒子問題（Black Box）**：
- 深度模型很難解釋「為什麼 NPC 此時決定放這招」，除錯與遊戲平衡（Game Balance）調整成本極高。

---

### 🍎 知識果實：架構師帶走的一句話
> **「傳統程式是人類教電腦『怎麼做』（How）；機器學習是人類告訴電腦『目標是什麼』（Goal），讓電腦自己摸索出路徑。」**

---
*註：目前 scratch 環境尚未初始化 git 儲存庫，無實體專案代碼需提交。若後續你需要將筆記或開發腳本落地成倉庫管理，隨時吩咐我立刻為你 `git init` 建立！*